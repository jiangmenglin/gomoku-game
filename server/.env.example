# Server port (default: 80 in production, 4000 in development)
PORT=80

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/gomoku

# JWT secret key (change to a random string in production)
JWT_SECRET=your_jwt_secret_key_here

# Client origin for CORS (leave empty in production for same-origin)
CLIENT_ORIGIN=
